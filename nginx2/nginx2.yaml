apiVersion: v1
kind: Pod
metadata:
  name: nginx2
  namespace: a-maxmudov
  labels:
    name: nginx2
    project: practice
    author: a-maxmudov
spec:
  containers:
  - name: nginx2
    image: nginx:latest
    resources:
    ports:
      - containerPort: 80
